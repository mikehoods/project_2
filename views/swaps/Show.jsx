const React = require('react')
const Layout = require('../components/Layout.jsx')

class ShowSwap extends React.Component {
    render() {
        return (
            <Layout>
                <h1>Look at this Swap</h1>
            </Layout>
        )
    }
}

module.exports = ShowSwap